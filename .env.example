# Optional default provider settings for local use only.
# You can also enter these in the app for the current browser session.
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=
OPENAI_ENDPOINT_MODE=chat.completions
PORT=8787
